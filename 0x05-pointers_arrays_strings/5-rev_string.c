#include "main.h"

/**
 * rev_string - a function that reverses a string
 * @s: input of string
 */
void rev_string(char *s)
{
	int length = 0;
	int count = 0;
	char temp;

	while (s[length] != '\0')
	{
		length++;
	}

	while (i < lenght)
	{
		lenght--;
		temp = s[i];
		s[i] = s[lenght];
		s[length] = remp;
		i++;
	}
}
