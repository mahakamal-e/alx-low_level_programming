#include "main.h"
/**
 * is_palindrome - function return 1 if string empty
 * @s: input string
 * Return: 1 if string empty
 * otherwise 0
 */
int is_palindrome(char *s)
{
	if (*s == 0)
		return(1);
}
